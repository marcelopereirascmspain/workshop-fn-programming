export const add = (x, y) => Number(x) + Number(y);
export const even = x => x % 2 === 0;
export const square = x => x * x;
export const inc = x => x + 1;
export const concat = (a, b) => String(a) + String(b);